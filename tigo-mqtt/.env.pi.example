# Raspberry Pi SSH credentials for cca-status.sh
# Copy this to .env.pi and fill in your values.
PI_HOST=<PI_HOST>            # e.g. raspberrypi.example.lan or a private LAN IP
PI_USER=solar-assistant
PI_PASS=your_password
