# Environment files with credentials (use .env.example / .env.pi.example as templates)
.env
.env.test
.env.pi
!.env.pi.example
!.env.example

# State and runtime data
data/
run/

# User-specific configs (generated by setup wizard)
config-*.ini
!config-template.ini
docker-compose.yml

# Topology backups (user-specific state data)
topology_backups/

# CCA state file backups (contain site-specific data)
backups/*/
