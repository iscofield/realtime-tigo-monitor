# Environment file with credentials (use .env.example as template)
.env
.env.test

# State and runtime data
data/
run/

# User-specific configs (generated by setup wizard)
config-*.ini
!config-template.ini
docker-compose.yml

# Topology backups (user-specific state data)
topology_backups/
